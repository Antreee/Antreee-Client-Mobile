import { View, Text } from "react-native";

function TabScreenTwoThree({ navigation }) {
   return (
      <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
         <Text>NewTab 3</Text>
      </View>
   );
}

export default TabScreenTwoThree